## Summary

Describe the problem and the smallest change that solves it.

## Verification

- [ ] I used synthetic fixtures only.
- [ ] I added or updated tests and watched new behavior tests fail before implementation.
- [ ] `ruff check` and `ruff format --check` pass.
- [ ] `mypy` and the full `pytest` suite pass.
- [ ] `scripts/check_public_tree.py` passes.
- [ ] I updated compatibility or threat-model documentation when behavior changed.
- [ ] I did not add databases, backups, runtime configuration, deployment files, private paths, raw logs, or credentials.

## Compatibility impact

List affected Hermes, Mnemosyne, Python, or operating-system boundaries. Write `none` if there is no impact.

## Security and privacy impact

Explain changes to stored data, recall boundaries, mutation policy, or dependencies. Write `none` if there is no impact.
